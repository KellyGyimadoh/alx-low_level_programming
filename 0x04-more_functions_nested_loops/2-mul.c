#include "main.h"
/**
 * mul - Multiples two integers
 * @x: The first number to multiply
 * @y: The second number to multiply with first number
 * Return: The result of the multiplication between x paran and y paran
 */
int mul(int x, int y)
{
	return (x * y);
}
